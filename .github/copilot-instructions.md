# CQC Registration Tracker

- Keep the UI simple, light, and mobile-first.
- Prefer reusable React components and local state for now.
- Use the mock checklist data in `src/data/checklist.ts` as the source of truth.
- Preserve the status labels exactly: `To do`, `Waiting`, and `Done`.
- Keep updates focused on the tracker flow and avoid adding unnecessary complexity.
